/* NOVARA Google Maps / Places configuration.
 * Add the browser-restricted Google Maps Platform key here after it is restricted
 * to the NOVARA production site and the required Google Maps/Places APIs.
 */
window.NOVARA_GOOGLE_MAPS_API_KEY = window.NOVARA_GOOGLE_MAPS_API_KEY || "";
